# Core analysis / exploration deps (used by scripts/explore_dataset.py).
# Training-specific deps (ultralytics, torch, etc.) will be added when the
# training pipeline lands.
pillow>=10.0,<12
numpy>=1.26,<3
matplotlib>=3.8,<4
PyYAML>=6,<7
